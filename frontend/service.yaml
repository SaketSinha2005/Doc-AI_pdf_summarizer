apiVersion: v1
kind: Service
metadata:
  name: my-service-frontend
spec:
  type: NodePort
  selector:
    app: app-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30009
